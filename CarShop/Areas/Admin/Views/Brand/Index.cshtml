@model List<Brand>

<div class="card shadow border-0 mt-4">
    <div class="card-header bg-secondary bg-gradient mt-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white">
                    Марки 
                </h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="row pt-4">
            <div class="col-12 text-end pb-4">
                <a asp-controller="Brand" asp-action="Create" class="btn btn-primary">
                    <i class="bi bi-plus-circle"></i> Добавить новую марку
                </a>
            </div>
        </div>

        <table class=" table table-bordered table-striped">
            <thead>
                <tr>
                    <th>
                        Brand Name
                    </th>
                    <th>
                        Display Order
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (Brand obj in Model.OrderBy(u => u.DisplayOrder))
                {
                    <tr>
                        <td style="width:250px">@obj.Name</td>
                        <td>@obj.DisplayOrder</td>

                        <td class="w-150" role="group">
                            <div>
                                <a style="width:300px" asp-controller="Brand" asp-action="Edit" asp-route-Id="@obj.Id" class="btn btn-secondary mx-2">
                                    <i class="bi bi-pencil-square"></i> Редактировать
                                </a>
                                <a style="width:300px" asp-controller="Brand" asp-action="Delete" asp-route-Id="@obj.Id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash3-fill"></i> Удалить
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
